<h1>Edit Resource for <em><%= @job.name %></em></h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for resources:</p>
    <ul>
      <li>Provide a link to an article, book, event, or something else relevant.</li>
      <li>Include pertinent and useful information for the community.</li>
    </ul>

    <p>
  </div>
  <div class="col-md-8">
    <%= form_for [@category, @job, @resource] do |f| %>
      <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: 'form-control', placeholder: "Enter resource title" %>
      </div>
      <div class="form-group">
        <%= f.label :link %>
        <%= f.text_field :link, class: 'form-control', placeholder: "Enter link" %>
      </div>      
      <div class="form-group">
        <%= f.label :body %>
        <%= f.text_area :body, rows: 5, class: 'form-control', placeholder: "Enter description" %>
      </div>
      <%= f.submit "Save", class: 'btn btn-success' %>
    <% end %>
  </div>
</div>