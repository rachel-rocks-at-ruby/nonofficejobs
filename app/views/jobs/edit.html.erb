<h1>Edit Job</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for jobs:</p>
    <ul>
      <li>Make sure the job is relevant.</li>
      <li>Contribute a post after submitting the job.</li>
      <li>Share the job with the world!</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @job do |f| %>
      <div class="form-group">
        <%= f.label :name %>
        <%= f.text_field :name, class: 'form-control', placeholder: "Enter job name" %>
      </div>
      <div class="form-group">
        <%= f.label :description %>
        <%= f.text_area :description, rows: 8, class: 'form-control', placeholder: "Enter job description" %>
      </div>
      <%= f.submit "Save", class: 'btn btn-success' %>
    <% end %>
  </div>
</div>